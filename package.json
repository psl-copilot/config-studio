{
  "name": "config-studio",
  "version": "1.0.0",
  "private": true,
  "description": "Config Studio - UI for managing Tazama configuration",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev:backend": "npm --workspace backend run start:dev",
    "dev:frontend": "npm --workspace frontend run dev",
    "build": "npm --workspace backend run build && npm --workspace frontend run build",
    "test": "npm --workspace backend run test && npm --workspace frontend run test",
    "test:cov": "npm --workspace backend run test:cov && npm --workspace frontend run test:cov",
    "lint": "npm --workspace backend run lint && npm --workspace frontend run lint"
  }
}
