// Route paths configuration
export const ROUTES = {
  // Public routes
  LOGIN: '/login',

  // Protected routes
  DASHBOARD: '/dashboard',
  NETWORK_MAP: '/network-map',
  RULE: '/rule',
  TYPOLOGY: '/typology',
} as const;
